import { Typography } from "@mui/material";

export default function Header() {
    return (
        <Typography
            color="primary"
            variant="h1"
        >
            Not So Bad
        </Typography>
    );
}
